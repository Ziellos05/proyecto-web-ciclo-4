import * as React from 'react';
import Button from '@mui/material/Button';

const RegisterScreen = () => {
  return <Button variant="contained">Register Screen</Button>;
}

export default RegisterScreen;
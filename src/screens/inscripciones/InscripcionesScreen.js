import * as React from 'react';
import Button from '@mui/material/Button';

const InscripcionesScreen = () => {
  return <Button variant="contained">Inscripciones Screen</Button>;
}

export default InscripcionesScreen;